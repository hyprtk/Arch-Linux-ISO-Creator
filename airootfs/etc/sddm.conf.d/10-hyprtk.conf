# hyprtk live-ISO SDDM defaults.
# Drop-ins in /etc/sddm.conf.d are merged alphabetically after /etc/sddm.conf.

[Autologin]
User=hyprtk
Session=hyprland
Relogin=false

[General]
# Hyprland is Wayland-only, so run the greeter on Wayland too (needs weston).
DisplayServer=wayland
Numlock=on
